import { Header } from "@/components/Header"
import { HeroMain } from "@/components/HeroMain"

export const HomePage = () => {
  return (
    <main>
      <Header/>
      <HeroMain/>
    </main>
  )
}
